Variable = require('lukeshay.utils.variable')
Keybind = require('lukeshay.utils.keybind')
Reload = require('lukeshay.utils.reload')
Command = require('lukeshay.utils.command')
Option = require('lukeshay.utils.option')

Vim = {
	Variable = Variable,
	Keybind = Keybind,
	Reload = Reload,
	Command = Command,
	Option = Option,
}
